package LinkedList;

class OCNode{
	
	int data;
	OCNode next;
	
	OCNode(int x){
		this.data=x;
		this.next=null;
	}
}

class OCList{
	
	OCNode head;
	
	OCList(){
		this.head=null;
	}
	
	public void push(int x) {
		
		OCNode temp = new OCNode(x);
		temp.next=head;
		head=temp;
	}
	
	public void display(CNode temp) {
		
		while(temp!=null) {
			System.out.print(temp.data);
			temp = temp.next;
		}
	}
	
	public void groupOddEvenNodes() {
		
		OCNode oddNode= this.head;
		OCNode evenNode = this.head.next;
		
		while(oddNode!=null && evenNode!=null) {			
		OCNode t1 = oddNode.next;
		OCNode t2=evenNode.next;
		OCNode temp = t2;
		t1=t2;
		t2=temp;
		oddNode=oddNode.next;
		evenNode=evenNode.next;
		}
		
		
	}
}
public class OddEvenLinkedList {

	public static void main(String[] args) {
	 OCList l = new OCList();
	 l.head=new OCNode(1);
	 l.head=new OCNode(2);
	 l.head=new OCNode(3);
	 l.display(l.head);
	 
	 
	 
}
}
